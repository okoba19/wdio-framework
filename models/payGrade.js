class PayGrade {
    constructor(currency, minSalary, maxSalary) {
      this.currency = currency;
      this.minSalary = minSalary;
      this.maxSalary = maxSalary;
    }
  }

  module.exports = PayGrade;